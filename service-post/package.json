{
  "name": "service-sport",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && npm run build:watch | nodemon dist/index.js",
    "build": "babel src --out-dir dist --source-maps --copy-files",
    "build:watch": "babel src --out-dir dist --source-maps --copy-files --watch"
  },
  "author": "Paz lazar",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "helmet": "^3.20.0",
    "mongodb": "^3.3.0",
    "mongoose": "^5.7.5",
    "mysql2": "^1.6.5",
    "sequelize": "^5.13.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5"
  }
}
